To use:

1) import all data as files into repo
2) clean data using combine_teams_data
3) generate rate data using getRate
4) then move on to PCA, then dendrogram, then cluster_summary, standardize_variables, then compare_season_data (or create bundles and use the R markdown document)
